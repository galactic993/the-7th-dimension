{
  "framework": "vite",
  "buildCommand": "export CONVEX_DEPLOY_KEY=$(echo \"$CONVEX_DEPLOY_KEY\" | sed 's/%7C/|/g') && npx convex deploy --cmd 'npm run build'",
  "devCommand": "npm run dev",
  "outputDirectory": "dist"
}